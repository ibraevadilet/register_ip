import 'package:flutter/material.dart';

class PinCodeEnterScreen extends StatelessWidget {
  const PinCodeEnterScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PinCodeEnterScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
