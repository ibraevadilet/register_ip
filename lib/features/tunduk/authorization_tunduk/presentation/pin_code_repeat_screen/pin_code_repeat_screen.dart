import 'package:flutter/material.dart';

class PinCodeRepeatScreen extends StatelessWidget {
  const PinCodeRepeatScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PinCodeRepeatScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
