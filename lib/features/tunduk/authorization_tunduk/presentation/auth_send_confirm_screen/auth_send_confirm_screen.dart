import 'package:flutter/material.dart';

class AuthSendConfirmScreen extends StatelessWidget {
  const AuthSendConfirmScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AuthSendConfirmScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
