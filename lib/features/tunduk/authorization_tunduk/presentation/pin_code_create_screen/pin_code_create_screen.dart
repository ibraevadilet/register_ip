import 'package:flutter/material.dart';

class PinCodeCreateScreen extends StatelessWidget {
  const PinCodeCreateScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PinCodeCreateScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}